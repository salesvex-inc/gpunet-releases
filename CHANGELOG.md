# Changelog

All notable, user-facing changes to the GPUNet Node desktop app and CLI are
recorded here. Each release entry corresponds to a tag in this repo
(`desktop-vX.Y.Z` / `node-vX.Y.Z`) and a matching GitHub Release.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/);
versions follow [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Added
- Initial public release & update distribution hub.
- Desktop app delivery via Tauri auto-updater (`latest.json`, minisign-signed).
- Node CLI binary delivery per platform.
